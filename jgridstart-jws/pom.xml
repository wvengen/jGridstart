<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
  xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <!-- package information -->
  <groupId>nl.nikhef.jgridstart</groupId>
  <artifactId>jgridstart-jws</artifactId>
  <version>1.10</version>
  <name>jGridstart Java Web Start</name>
  <url>http://jgridstart.nikhef.nl/</url>
  <description>Java web start deployment for jGridstart</description>
  <packaging>pom</packaging>

  <parent>
    <groupId>nl.nikhef.jgridstart</groupId>
    <artifactId>jgridstart</artifactId>
    <version>1.0</version>
    <relativePath>../pom.xml</relativePath>
  </parent>

  <properties>
    <deployurl>http://www.nikhef.nl/~wvengen/jgridstart-devel/</deployurl>
    <mainClass>nl.nikhef.jgridstart.gui.Main</mainClass>
  </properties>

  <!-- just package the jGridstart application -->
  <dependencies>
    <dependency>
      <groupId>nl.nikhef.jgridstart</groupId>
      <artifactId>jgridstart-small</artifactId>
      <version>1.10</version>
    </dependency>
  </dependencies>

  <build>
    <plugins>
      <!-- jnlp generation -->
      <plugin>
        <groupId>org.codehaus.mojo.webstart</groupId>
	<artifactId>webstart-maven-plugin</artifactId>
	<configuration>
          <jnlpFiles>
            <jnlpFile>
              <templateFilename>jgridstart.vm</templateFilename>
	      <outputFile>jgridstart.jnlp</outputFile>
              <jnlpResources><jnlpResource>
	        <mainClass>${mainClass}</mainClass>
              </jnlpResource></jnlpResources>
            </jnlpFile>
          </jnlpFiles>
          <!-- TODO bcprov.jnlp -->
	</configuration>
      </plugin>

      <!-- ordinary testing is done already in build step, skip now -->
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-surefire-plugin</artifactId>
        <configuration><skipTests>true</skipTests></configuration>
      </plugin>
    </plugins>
  </build>

  <!-- don't build JavaDoc -->
  <reporting>
    <plugins>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-javadoc-plugin</artifactId>
        <version>2.7</version>
        <configuration><skip>true</skip></configuration>
      </plugin>
    </plugins>
  </reporting>

</project>
